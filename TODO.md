# TODO: Add Login and Logout Pages with MongoDB Authentication

## Backend Updates

- [x] Add `pymongo` and `flask-jwt-extended` to `heart-backend/requirements.txt`
- [x] Update `heart-backend/app.py` to connect to MongoDB, add user collection, and create `/register`, `/login`, `/logout` routes

## Frontend Updates

- [x] Create `heart-frontend/app/login/page.tsx` for login form
- [x] Update `heart-frontend/app/layout.tsx` to add logout button and functionality

## Followup Steps

- [x] Install backend dependencies (`pip install -r heart-backend/requirements.txt`)
- [x] Install frontend dependencies if needed (`npm install` in heart-frontend)
- [x] Test authentication flow: register, login, logout
